using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BossSoundFx : SoundFx
{
    public enum BOSSAUDIO { ATTACK1, ATTACK2, MAGIC1, MAGIC2, TELEPORTSTART, TELEPORTEND, PHASESHIFT, DIE };
}
